elasticsearch-master-0apiVersion: argoproj.io/v1alpha1

kind: Application

metadata:

  name: samp

  namespace: argo

  finalizers:

  - resources-finalizer.argocd.argoproj.io

spec:

  destination:

    namespace: argo

    server: https://kubernetes.default.svc

  syncPolicy:
    automated:
      prune: true
      selfHeal: true


  project: default

  source:

    path: nginx

    repoURL: https://github.com/krupakar0307/helm.git
    
    targetRevision: HEAD
